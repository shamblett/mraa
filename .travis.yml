os: linux
dist: trusty
language: dart
dart:
  # Install the latest stable release
  - stable
dart_task:
  - test: --exclude-tags no-xvfb
    xvfb: false
script:
  pub run test test/mraa_test.dart

branches:
    only:
    - master

